/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['nftcollectionuploads.s3.amazonaws.com', 'nftuploads2.s3.amazonaws.com'],
    },
};

export default nextConfig;